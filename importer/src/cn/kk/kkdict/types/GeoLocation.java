package cn.kk.kkdict.types;

import cn.kk.kkdict.utils.Helper;

public enum GeoLocation {
    ;
    public static final String TYPE_ID = "位";
    public static final byte[] TYPE_ID_BYTES = TYPE_ID.getBytes(Helper.CHARSET_UTF8);
}
